'use strict';

Object.defineProperty(exports, "__esModule", {
  value: true
});

var converter = function converter(characters) {
  var chars = characters.split('');
  var n = chars.length;

  var convert = function convert(x, name) {
    return x < 0 ? name : convert(x / n - 1, chars[Math.floor(x % n)] + name);
  };

  return function (x) {
    return convert(x, '');
  };
};

exports.default = converter;